pipeline {
    agent any
    stages {
        stage('Verify Docker') {
            steps {
                script {
                    // Ensure Docker is accessible
                    sh 'docker --version'
                }
            }
        }
        stage('Build') {
            steps {
                sh 'docker inspect -f . abhishekf5/maven-abhishek-docker-agent:v1'
            }
        }
    }
}
